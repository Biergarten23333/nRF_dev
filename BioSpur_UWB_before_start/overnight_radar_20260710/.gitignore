# Regenerable bulk data — exceeds GitHub's 100 MB/file limit. Kept on the SSD
# only; not version-controlled. Everything else (scripts, report, figures,
# templates/, small JSON/CSV summaries) IS committed.
#
#   raw/*.log            600-690 MB each  (6 raw listener serial captures, 4.0 GB)
#   analysis/parsed/*    ~386 MB CIR .npy + parsed npz  (1.9 GB) -- regenerate
#                        from raw/ via analysis/step0_parse.py
raw/
analysis/parsed/
