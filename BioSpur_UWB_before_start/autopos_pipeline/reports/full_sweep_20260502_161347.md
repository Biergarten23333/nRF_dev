# Full Sweep Run - 2026-05-02 16:13:47

## Command

```bash
python3 scripts/run_autopos_sweep_loop.py \
  --port /dev/serial/by-id/usb-Master_Anchor_BioSpur_BLE_Control_87EA2F4A526C5A02-if00 \
  --order ABCDEFGH \
  --sw-sets 1 \
  --prewarm-sw-sets 0 \
  --warmup-min-quality 0 \
  --verbose 1 \
  --quiet-tag-name - \
  --reuse-resident-anchor-master \
  --no-bootstrap-autopos-reset \
  --round-retries 1 \
  --out-dir autopos_pipeline/logs/full_sweep_20260502_161347
```

## Log Paths

- Sweep log root: `autopos_pipeline/logs/full_sweep_20260502_161347`
- Session matrix guard: `autopos_pipeline/logs/full_sweep_20260502_161347/session_role_guard.log`
- Round A log: `autopos_pipeline/logs/full_sweep_20260502_161347/round_A/master.log`
- Round B log: `autopos_pipeline/logs/full_sweep_20260502_161347/round_B/master.log`
- Post-abort responder restore: `autopos_pipeline/logs/anchor_ready_after_full_sweep_abort_20260502_161759`

## Outcome

Partial run only. The sweep did not complete A-H.

- Session role guard succeeded before the sweep:
  - action: `anchor role all matrix`
  - ready: `8/8`
  - final role counts: `matrix=8 responder=0 master=0 other=0`
- Round A completed and produced one SW line:
  - `[AUTOPOS] SW-A,B,0,0,C,0,0,D,0,0,E,0,0,F,0,0,G,0,0,H,0,0`
- Round A warnings:
  - SW observed without `AUTOPOS apply success`
  - no matrix output for B-H during SW-A
- Round B did not produce an SW-B line.
- The process was stopped manually after Round B stayed in `stage=sweeping` for more than 200 seconds with 0% progress.
- A-H responder runtime was restored after abort:
  - command sent: yes
  - ack ok: yes
  - ready: `8/8`

## Key Evidence

Round B starts with stale/old A state:

```text
AUTOPOS round staged: master=B sets=1
AUTOPOS: mode=AUTOPOS state=staged staged=B last_success=B sets=1 error=-
autopos apply already running
```

Later, while preparing SW-B, the log still shows SW-A and A's sweep done:

```text
SW-A,B,0,0,C,0,0,D,0,0,E,0,0,F,0,0,G,0,0,H,0,0
SWEEP_DONE master=A sets=1 role=matrix
AUTOPOS anchor B role verified
AUTOPOS apply success: master=B
PRECHECK WARN: stale SW-A observed while preparing SW-B; finite-master image should auto-return it to matrix
AUTOPOS sweep listen attach: master=B uuid=B9179575C776C98F1CB132DD6EDC6223
```

After that point, no `SW-B` line was observed before the run was stopped.

## Current State

The system has been restored to normal responder mode after the aborted sweep:

```text
anchor role all responder runtime final sent=8 ready=8/8
```

No firmware build, flash, or OTA operation was performed during this run.

## Follow-Up Comparison Against Known-Good Sweep

Known-good AutoPos sweeps were found at:

- `logs/autopos_then_3tag_capture_20260426_225235/autopos/summary.json`
- `logs/autopos_then_3tag_capture_20260426_230050/autopos/summary.json`

Important differences:

- Known-good runs used `prewarm_sw_sets=10`, `sw_sets=10`, `device_sw_sets=20`.
- This failed probe used `prewarm_sw_sets=0`, `sw_sets=1`, `device_sw_sets=1`.
- Known-good Anchor firmware in the AutoPos state lines was `anchor-runtime-force-20260426_2`.
- This failed probe used Anchor firmware `alt-bcast-a13-nosleep-hotpath-g`.
- Known-good round logs show `SW-X warmup discard 1/10 ... 10/10`, then kept nonzero SW rows.
- This failed probe accepted the first SW-A row directly, and that row was all zeros.

Interpretation:

1. The all-zero SW-A row can plausibly be a cold-start / non-warmed-up row because this run disabled the 10-row prewarm that the known-good flow used.
2. The SW-B hang is probably not explained by warmup alone. The log also shows `autopos apply already running`, stale `SW-A`, and the current Anchor image differs from the known-good AutoPos image.
3. The next diagnostic should first re-run the known-good host parameters on the current a13 Anchor image before changing firmware:

```bash
python3 scripts/run_autopos_sweep_loop.py \
  --port /dev/serial/by-id/usb-Master_Anchor_BioSpur_BLE_Control_87EA2F4A526C5A02-if00 \
  --order ABCDEFGH \
  --sw-sets 10 \
  --prewarm-sw-sets 10 \
  --warmup-min-quality 90 \
  --verbose 1 \
  --quiet-tag-name - \
  --reuse-resident-anchor-master \
  --no-bootstrap-autopos-reset \
  --round-retries 1 \
  --out-dir autopos_pipeline/logs/full_sweep_known_good_params_$(date +%Y%m%d_%H%M%S)
```
