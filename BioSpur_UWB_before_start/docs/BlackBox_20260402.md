# BlackBox 2026-04-02

Date: 2026-04-02  
Scope: strict OTA `-116` blocker iterative fix loop (build -> flash -> OTA test), with A/D/H comparison follow-up and anchor-H deep debug.

## 1) Starting point

- Resumed from prior breakpoint where strict target gating was already converged, but OTA execution still failed:
  - `DFU SMP service ready`
  - first SMP command timeout:
    - `OTA command wait failed ... rc=-116`
    - `OTA wait debug: rsp_len=0 rsp_total=0 chunk=0 off=0`

## 2) Iteration changes applied (today)

### 2.1 Controller-side scan/disconnect hardening (apps/master_ota/src/main.c)

Applied patch:
- Stop scan before discovery immediately after connect.
- In `disconnected()`:
  - ignore non-`default_conn` disconnect events,
  - prevent stale disconnect from resetting active OTA state / restarting scan.

Rationale:
- Prior logs showed scan/disconnect noise could race OTA flow.

Build/flash evidence:
- `logs/build_master_control_unified_cdc_20260402_iter2.log`
- `logs/flash_52840_unified_cdc_20260402_iter2.log`

### 2.2 Controller-side security-gated OTA attempt (apps/master_ota/src/main.c)

Applied patch:
- add `security_changed` callback + semaphore.
- request `BT_SECURITY_L2` on connect.
- wait briefly for encrypted link before DFU discovery/start.

Build/flash evidence:
- `logs/build_master_control_unified_cdc_20260402_iter3.log`
- `logs/flash_52840_unified_cdc_20260402_iter3.log`

## 3) Iteration test results

### 3.1 Strict OTA trial on H (post-iter2 / post-iter3)

Runs:
- `logs/debug_ota_iter2_H_20260402.runner.log`
- `logs/debug_ota_iter2_H_20260402/trial_01/ota_trial.log`
- `logs/debug_ota_iter3_H_20260402.runner.log`
- `logs/debug_ota_iter3_H_20260402/trial_01/ota_trial.log`

Result:
- strict identity targeting still correct.
- OTA execution still fails (`-116`), not converged.

### 3.2 Critical runtime mismatch discovered

Observed from runtime logs:
- active control port (`usb-BioSpur_BioSpur_BLE_Control_8D3AC42D4D90FAE8-if00`) still prints:
  - `Security request: skipped (OTA uses current link security)`
- but patched ELF contains new strings:
  - `Security request: L2 (rc=%d)`
  - `Security request failed: %d`

Evidence:
- runtime output with old string:
  - `logs/manual_ota_probe_iter3.log`
  - `logs/debug_ota_iter3_H_20260402/trial_01/ota_trial.log`
- patched binary strings:
  - from `build-master-control-unified-cdc-20260401e/master_control/zephyr/zephyr.elf`

Conclusion:
- the currently active USB CDC runtime device is not taking the flashed J-Link `683234364` image path.
- this is now a hard deployment-path blocker for validating controller-side code fixes.

## 4) Anchor-H instrumentation and compare-focused debug

### 4.1 Reflash Anchor H with OTA-diag image

Flash:
- `scripts/reset_then_flash.sh 760186121 build-anchor-unified-ota-diag-20260401/merged.hex`

Evidence:
- `logs/iter4_flash_H_ota_diag_20260402.log`
- boot proof:
  - `logs/iter4_H_boot_after_ota_diag_flash_20260402.log`
  - includes:
    - `anchor mcumgr diag callbacks registered`
    - `DEVICE_UUID: 1EABFBEC28B8053FBB0D5C448112AE93`

### 4.2 Dual-serial capture (controller + anchor H)

Runs:
- before H diag reflash:
  - `logs/debug_dual_serial_H_iter4_20260402.log`
- after H diag reflash:
  - `logs/debug_dual_serial_H_after_diagflash_iter4_20260402.log`

Findings:
- Controller reaches:
  - `DFU SMP service ready`
  - sends SMP commands
  - times out `-116`
- Anchor H serial shows BLE control link connect/disconnect events, but no MCUmgr request-event evidence during timeout window.

Interpretation:
- consistent with request not being accepted by target-side ATT/SMP path (or dropped before MCUmgr handler), matching `rsp_len=0` symptom.

## 5) Build-path blocker hit on anchor-side quick mitigation rebuild

Attempted non-sysbuild anchor OTA rebuild (to enforce `MCUMGR_TRANSPORT_BT_PERM_RW=n`) failed in current workspace context:
- no west workspace context for `west build`,
- mcuboot signing/regeneration path blocked (`west` workspace expectation in this tree mode).

Evidence:
- `logs/build_anchor_ota_nosys_20260402g.log`
- `logs/build_anchor_ota_nosys_20260402_iter2.log`
- `logs/build_anchor_ota_nosys_20260402_iter3.log`

## 6) Current status at end of session

- Fixed in source:
  - scan/disconnect race guards in `apps/master_ota/src/main.c`
  - security-gated OTA attempt in `apps/master_ota/src/main.c`
- Still blocked in live runtime:
  - active USB-CDC controller target not yet confirmed to run patched image.
- Strict target safety remains converged.
- Strict target OTA execution convergence remains **not achieved** (`-116` persists).

## 7) Next required action (carry-forward)

1. Resolve controller deployment-path mismatch first:
   - identify exact flash/update path for active `usb-BioSpur_BioSpur_BLE_Control_*` runtime target.
2. Re-run strict OTA trial after confirmed patched runtime.
3. Keep dual-serial capture enabled on Anchor H to verify MCUmgr request visibility.
4. Once H path responds, repeat on A and D for compare closure.

## 8) Additional late-session checks

- Attempted to validate patched firmware via J-Link VCOM path (`/dev/serial/by-id/usb-SEGGER_J-Link_000683234364-if00`) with loop runner:
  - run hung without usable device output (host commands logged, no runtime lines).
  - indicates this VCOM path is not the active operational control console used in strict runs.
- Attempted “reset H then trial” loop:
  - `nrfjprog --reset --snr 760186121` became unstable/hung in this environment during chained run.
  - run aborted to avoid false-positive conclusions from transport tooling instability.

Practical status after all iterations today:
- **Safety gate:** still good.
- **Execution convergence:** still blocked (`-116`) and not fix-closed.

## 9) Deployment-path root blocker (confirmed)

New checks completed:

1. Verified active runtime USB device identity:
   - `lsusb` shows a separate device:
     - `2fe3:1002 NordicSemiconductor BioSpur_BLE_Control`
   - active control port in strict runs:
     - `/dev/serial/by-id/usb-BioSpur_BioSpur_BLE_Control_8D3AC42D4D90FAE8-if00`

2. Verified flashed image contents vs runtime output mismatch:
   - patched ELF contains:
     - `Security request: L2 (rc=%d)`
   - active runtime still prints:
     - `Security request: skipped (OTA uses current link security)`

3. Checked standard J-Link reset relation:
   - `nrfjprog --reset --snr 683234364` in this environment frequently hangs/errors (`JLinkARM.dll -256`),
   - and does not demonstrate controlled re-enumeration coupling for `usb-BioSpur_BLE_Control_*`.

Conclusion:
- The active `usb-BioSpur_BLE_Control_*` controller runtime is not yet on the same confirmed update path as current `flash_master_noninteractive.sh` (SN `683234364`) workflow.
- Until this path is unified/confirmed, code-level controller OTA fixes cannot be reliably validated on the actual running controller.

## 10) Carry-forward immediate requirement

Before next strict OTA fix iteration:
- establish the exact flash/update path for:
  - `usb-BioSpur_BioSpur_BLE_Control_8D3AC42D4D90FAE8-if00`
- then rerun:
  - H strict trial (target UUID fixed)
  - A strict trial
  - D strict trial
with dual-serial capture and compare.

## 11) Iterative strict-OTA fix continuation (2026-04-02 evening)

### 11.1 Confirmed active controller build/runtime path

- Reflashed DK SN `683234364` explicitly (no probe selection) with:
  - `build-master-control-unified-cdc-20260401e/merged.hex`
- Flash command (explicit SNR):
  - `nrfjprog --snr 683234364 --program ... --chiperase --verify --reset`

Evidence:
- `logs/debug_ota_iter4_H_20260402/flash_52840_cdc_e.log`

Result:
- runtime on `usb-BioSpur_BioSpur_BLE_Control_8D3AC42D4D90FAE8-if00` now shows new menu/state lines from updated control firmware.

### 11.2 Strict-target state machine hardening (controller side)

Two concrete guards added in `apps/master_ota/src/main.c`:

1. `connected()` now rejects non-`default_conn` links:
- logs `Connected ignored (not default_conn): ...`
- disconnects unsolicited links immediately

2. `scan_connecting()` no longer steals ownership of `default_conn`:
- prevents scan-layer implicit connection events from poisoning strict target identity state.

Build/flash evidence:
- `logs/build_master_control_unified_cdc_20260402f_unsolicited_guard.log`
- `logs/build_master_control_unified_cdc_20260402f_scanconn_guard.log`
- `logs/debug_ota_iter4_H_20260402/flash_52840_cdc_e_unsolicited_guard.log`

### 11.3 Reproduced and narrowed blocker with strict UUID targeting

Target UUID (Anchor H):
- `1EABFBEC28B8053FBB0D5C448112AE93`

After guards:
- strict UUID filtering works and rejects non-targets explicitly
- target match connect path succeeds
- state progression reaches:
  - `target_configured`
  - `connect_pending`
  - `connected_verified`
  - `ota_uploading`

Evidence:
- `logs/debug_ota_iter7_H_20260402/trial_01/ota_trial.log`
- key lines include:
  - `Scan eval: E3:77:2C:61:AA:19 ... uuid_ok=1 strict_id=1`
  - `Connected target evidence: verified=1 uuid=1EAB...`
  - `DFU SMP service ready`
  - `OTA_STATE:ota_uploading detail=start`

### 11.4 Remaining hard blocker (post-connect OTA command response)

Even after successful strict target connect + DFU discovery:
- first SMP echo command times out:
  - `[2550->32550] OTA command wait failed ... rc=-116`
- erase command also times out:
  - `[32552->62552] OTA command wait failed ... rc=-116`
- `rsp_len=0 rsp_total=0`

Evidence:
- `logs/debug_ota_iter7_H_20260402/trial_01/ota_trial.log`
- runner summary:
  - `logs/debug_ota_iter7_H_20260402/runner.log`

Interpretation:
- wrong-target and connect-ambiguity are now largely controlled;
- current blocker is now in **SMP command/response execution** after verified connect.

### 11.5 Security-path adjustment for anchor profile

Controller behavior updated:
- for `device kind anchor` (NUS disabled path): skip L2 security request (`Security request: skipped (anchor OTA profile)`)
- for `device kind tag`: keep L2 request path.

Reason:
- L2 request was causing frequent `err=5` disconnects on anchor path.
- anchor strict path now reaches `ota_uploading` reliably, but still blocked by SMP response timeout (`-116`).

### 11.6 Additional settle-window experiment (iter8)

Change:
- increased post-DFU-discovery settle delay from `300 ms` to `1200 ms` before first SMP command.

Evidence:
- build: `logs/build_master_control_unified_cdc_20260402f_settle1200.log`
- flash: `logs/debug_ota_iter8_H_20260402/flash_52840.log`
- trial: `logs/debug_ota_iter8_H_20260402/trial_01/ota_trial.log`
- summary: `logs/debug_ota_iter8_H_20260402/runner.log`

Result:
- no behavioral improvement on completion:
  - strict target verification still succeeds,
  - OTA still reaches `ota_uploading`,
  - first SMP commands still time out (`-116`).

Conclusion:
- the remaining blocker is not a simple ATT-settle timing issue.
