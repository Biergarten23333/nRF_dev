# BlackBox 2026-03-31

## Session Policy
- From this point onward, all actions on 2026-03-31 are logged in this file.

## 1) Tag115 PMODE switch verification via 52840 (executed)

### Scope
- Objective: switch Tag115 (`BSF66F`, `tag=1`) from `PMODE=0` to `PMODE=1` with fixed anchors `A,B,C,D` (`0,1,2,3`).
- Control path used in this run: **one-shot command embedded in 52840 master build**, then sent over BLE NUS after connect.

### Build artifacts used
- `build-master-oneshot-modeq-20260331b`
- `build-master-oneshot-pmode1-abcd-20260331`
- `build-master-oneshot-cfgstatus-20260331`

### Flash/runtime notes
- `scripts/flash_master_noninteractive.sh` / `scripts/reset_then_flash.sh` path showed hang/timeout in this environment.
- Successful fallback used: SN-pinned direct JLink flash to 52840:
  - `JLinkExe -NoGui 1 -SelectEmuBySN 683234364 -CommanderScript ...`
- No anchor flashing involved in this operation.

### Evidence logs
- Before switch (`MODE?`):
  - `logs/tag_mode_switch_20260331_pmode1_abcd/02c_modeq_before_serial_biospur.log`
  - Key line: `MODE=MOTION PMODE=0 AMODE=0 FIXED_N=0 ...`
- Apply switch (`CFG ... PMODE=1 FIXED=0,1,2,3`):
  - `logs/tag_mode_switch_20260331_pmode1_abcd/04_pmode1_apply_serial_biospur.log`
  - Key line: `CFG_OK TAG=1 SLOT=0/10 PERIOD=25 ACTIVE=20 GEN=1 LIVE=1`
- After switch (`MODE?`):
  - `logs/tag_mode_switch_20260331_pmode1_abcd/06_modeq_after_serial_biospur.log`
  - Key line: `MODE=FIXED PMODE=1 AMODE=1 FIXED_N=4 ... SRC=MASTER`
- Explicit readback (`CFG_STATUS`):
  - `logs/tag_mode_switch_20260331_pmode1_abcd/09b_cfgstatus_serial_biospur.log`
  - Key line: `CFG ... pmode=1 amode=1 fixed=0,1,2,3`

### Result
- PMODE switch objective achieved and verified:
  - `PMODE: 0 -> 1`
  - `fixed anchors: 0,1,2,3`
- Important clarification:
  - This run used 52840 reflashing for one-shot command.
  - Runtime bridge path (`Serial -> 52840 -> BLE`, no reflashing) is not yet implemented in this step.

## 2) OTA build name lookup (executed)

### User ask
- "name of the OTA build (OTA only, blank OTA)"

### Current build names reported
- Tag OTA-only (quiet OTA stream gate build):
  - `build-tag-ble-unified-otaquiet-20260331`
- Matching uploader/master side used for OTA verify:
  - `build-master-ota-tag115-quietverify-20260331`

## 3) Unified-anchor strict current-state review (repo-only, no code change)

### Scope
- Requested review basis: GitHub repo `Biergarten23333/nRF_dev` content only.
- Local checkout used for evidence: `/home/zekaixiao/Documents/nRF_dev` (origin points to the same repo).
- Working mode: read-only audit (no source modifications).

### Audit inputs checked
- Core unified-anchor code:
  - `apps/anchor/src/anchor_app.c`
  - `src/anchors/unified/anchor_config.{h,c}`
  - `src/anchors/unified/anchor_ble_id.{h,c}`
  - `src/anchors/unified/uart_role_switch.{h,c}`
- Host scripts:
  - `scripts/build_anchor_unified.sh`
  - `scripts/build_anchor_unified_role.sh`
  - `scripts/provision_anchor.py`
  - `scripts/read_config.py`
  - `scripts/scan_and_map.py`
  - `scripts/serial_switch_role.py`
  - `scripts/flash_anchor_auto.sh`
- CI/workflow:
  - `.github/workflows/anchor-unified-build.yml`
- Validation/docs:
  - `README.md`
  - `docs/BlackBox_20260330.md`
  - `docs/current_autopositioning_baseline.md`
  - representative logs under:
    - `logs/phase21_unified_anchor_20260330_092822/`
    - `logs/phase21b_run_20260330_130955/`
    - `logs/phase21_fieldtest_20260330_104241/matrix/`
    - `logs/phase21_whole_cycle_20260330_160828/`

### Output target
- Prepare evidence-based “implemented vs verified” status breakdown for:
  - unified build path
  - role dispatch
  - provisioning/readback
  - config schema
  - BLE identification/discovery
  - BLE naming/identity
  - UART serial path
  - startup signature
  - helper scripts
  - integrated validations

## 4) Unified Anchor BLE-first control plane (implemented + validated)

### Scope
- Implement anchor-side formal BLE config/control plane on top of existing unified-anchor baseline.
- Do not redo role runtime, provisioning baseline, BLE discovery baseline, or UART fallback architecture.
- Keep identity separation:
  - stable physical ID (`device_uuid`)
  - logical label (`U` / `A..H`)
  - display/name (`BSxxxx`).

### Code changes
- Config schema and lifecycle:
  - `src/anchors/unified/anchor_config.h`
  - `src/anchors/unified/anchor_config.c`
  - Added schema/version/generation/label model for BLE-safe remote control.
  - Added v2 validation + legacy v1 read compatibility.
- BLE control service (new):
  - `src/anchors/unified/anchor_ble_ctrl.h`
  - `src/anchors/unified/anchor_ble_ctrl.c`
  - New custom service UUID: `2f2b8f40-84e0-4be6-b6bf-2fd95f39d3f0`
  - Characteristics:
    - state `...d3f1` (read/notify)
    - active cfg `...d3f2` (read)
    - pending cfg `...d3f3` (read)
    - control `...d3f4` (write / write-without-response)
    - result `...d3f5` (read/notify)
  - Safe control semantics:
    - write pending config
    - validate
    - commit (persist + verify)
    - reboot
    - explicit status/errors (`ERR:BUSY`, `ERR:INVALID_CONFIG`, `ERR:WRITE_FAIL`, `ERR:CRC_MISMATCH`, etc.)
- Runtime integration:
  - `apps/anchor/src/anchor_app.c`
  - `apps/anchor/CMakeLists.txt`
  - `apps/anchor/prj.conf`
  - Added BLE control init + busy/runtime state hooks.
  - Added unassigned/unset mode behavior: control plane active while ranging is not started.
  - `CONFIG_BT_MAX_CONN=2` to avoid bench conflict where one central connection blocked BLE control.
- BLE ID and UART compatibility updates:
  - `src/anchors/unified/anchor_ble_id.c`
  - `src/anchors/unified/uart_role_switch.c`
- Host tools:
  - `scripts/ble_anchor_control.py` (new BLE control helper)
  - `scripts/provision_anchor.py` (v2 schema support incl. `U`/`unset` + generation)
  - `scripts/read_config.py` (v2+legacy readback)
  - `scripts/scan_and_map.py` (label decode updates)

### Build/flash evidence
- Build artifacts:
  - `build-anchor-unified-blectrl-20260331`
  - `build-anchor-unified-blectrl-20260331c`
  - `build-anchor-unified-blectrl-20260331d`
  - `build-anchor-unified-blectrl-20260331e`
- Build logs:
  - `logs/anchor_ble_ctrl_phase_20260331_1945/build_unified_conn2.log`
  - `logs/anchor_ble_ctrl_phase_20260331_1945/build_unified_writeprop.log`
  - `logs/anchor_ble_ctrl_phase_20260331_1945/build_unified_shortcmd.log`
- Flash logs (Anchor B SN `760185876`):
  - `logs/anchor_ble_ctrl_phase_20260331_1945/flash_B_unified.log`
  - `logs/anchor_ble_ctrl_phase_20260331_1945/flash_B_writeprop.log`
  - `logs/anchor_ble_ctrl_phase_20260331_1945/flash_B_shortcmd.log`

### Validation A: single-anchor BLE read/write/apply/readback loop (real)
- Target anchor stable UUID: `B9179575C776C98F1CB132DD6EDC6223` (BS592A)

1. Start from unassigned/unset baseline:
   - `logs/anchor_ble_ctrl_phase_20260331_1945/provision_B_to_U_for_ble_commit.json`
   - `logs/anchor_ble_ctrl_phase_20260331_1945/ble_loop_B917_read_after_provisionU.json`
   - Key state: `label=U role=unset busy=0 schema=2 gen=10`

2. BLE write pending + validate + commit:
   - `logs/anchor_ble_ctrl_phase_20260331_1945/ble_loop_B917_set_matrix_commit.json`
   - Commands/result:
     - `R MATRIX -> OK PENDING_ROLE`
     - `L B -> OK PENDING_LABEL`
     - `G 11 -> OK PENDING_GEN`
     - `VALIDATE -> OK VALID`
     - `COMMIT -> OK COMMIT REBOOT_REQUIRED`

3. Apply via reboot and readback:
   - BLE reboot command was issued successfully (write path).
   - Post-apply scan/readback:
     - `logs/anchor_ble_ctrl_phase_20260331_1945/ble_scan_after_reboot_B917.json`
     - `logs/anchor_ble_ctrl_phase_20260331_1945/ble_loop_B917_read_after_reboot.json`
   - Key readback: `label=B role=matrix schema=2 gen=11`.

### Validation B: explicit busy-path behavior (real)
- Busy rejection evidence:
  - `logs/anchor_ble_ctrl_phase_20260331_1945/ble_loop_B917_write_commit.json`
- Key result:
  - state showed `busy=1` while ranging role active.
  - `COMMIT -> ERR:BUSY` as designed.

### Validation C: multi-anchor distinguishability + targeted write direction (real)
- Multi-anchor concurrent scan evidence before and after B-targeted control:
  - Before write/apply baseline:
    - `logs/anchor_ble_ctrl_phase_20260331_1945/ble_scan_latest.json`
    - shows B UUID `B917...6223` at `label=U role=unset`
    - and C UUID `AB14...394B` at `label=C role=responder`
  - After B write/apply:
    - `logs/anchor_ble_ctrl_phase_20260331_1945/ble_scan_after_reboot_B917.json`
    - B changed to `label=B role=matrix`
    - C remains `label=C role=responder`
- This confirms:
  - unique stable identity remains distinguishable in multi-anchor environment
  - targeted config operation changed intended anchor only
  - label change did not break stable identity mapping.

### Validation D: unassigned -> assigned flow (real)
- Same anchor (B UUID `B917...6223`) transitioned:
  - `U/unset` (gen=10) -> `B/matrix` (gen=11)
  - via BLE pending/validate/commit/apply/readback path.

### Notes / limitations in this phase
- BLE control plane on anchor side is implemented and working for safe config lifecycle.
- Occasional BlueZ connection volatility was observed on some non-target anchors in bench conditions; validation logs use stable UUID + scan evidence to avoid false attribution.
- `nrfjprog --reset` on B showed environment-specific `-256` hang once:
  - `logs/anchor_ble_ctrl_phase_20260331_1945/reset_B_after_commit.log`
  - final apply verification did not depend on this path (BLE reboot + readback used).
- UART path remains fallback/debug; BLE is now the formal control direction.

## 5) Anchor OTA + 52840 control-center direction (in progress)

### Build/bundle implementation
- Added OTA-capable anchor build profile:
  - `apps/anchor/prj_ota.conf`
  - `apps/anchor/pm_static.yml` (keeps `anchor_cfg` region at `0x7e000`).
- Added/updated FW marker in anchor startup/control state:
  - `apps/anchor/CMakeLists.txt` (`APP_ANCHOR_FW_MARKER`)
  - `apps/anchor/src/anchor_app.c`
  - `src/anchors/unified/anchor_ble_ctrl.h`
  - `src/anchors/unified/anchor_ble_ctrl.c`
- Added OTA+control bundle build script:
  - `scripts/build_anchor_ota_control_bundle.sh`
- Extended 52840 `master_control` toward system-object model:
  - `apps/master_control/src/main.c`
  - added `device show`, `device kind <anchor|tag>` and capability view.

### Build evidence
- Final successful OTA/control bundle build:
  - `build-anchor-unified-ota-20260331f`
  - `build-master-control-anchor-ota-20260331f`
  - log: `logs/anchor_ota_phase_20260331_build_bundle_f.log`
- Toolchain unblock:
  - NCS Python lacked SSL/pip; used `PYTHONPATH=/usr/lib/python3/dist-packages` so imgtool could import `cryptography`.
- Additional marker-distinct OTA baseline build:
  - `build-anchor-unified-ota-pre-20260331`
  - log: `logs/anchor_ota_phase_20260331_build_pre.log`

### Flash / execution evidence
- Anchor B pre-OTA flash (SN `760185876`):
  - `logs/anchor_ota_phase_20260331_flash_anchorB_pre_marker.log`
- 52840 control build flash (SN `683234364`):
  - `logs/anchor_ota_phase_20260331_flash_52840_control.log`
  - `logs/anchor_ota_phase_20260331_flash_52840_control_rerun.log`
  - `logs/anchor_ota_phase_20260331_flash_52840_postota_reset.log`

### OTA run evidence (real, from 52840 control build)
- Log: `logs/anchor_ota_phase_20260331_ota_run.log`
- Sequence observed:
  - control commands issued over 52840 UART (`device kind anchor`, `ota_target name ...`, `mode ota`)
  - OTA mode launched
  - SMP DFU connected
  - upload from 0% to 100%
  - `OTA upload complete`
  - pending/test + reset command sequence sent
  - disconnect after reset

### Identity/config persistence check across OTA
- Pre/post BLE scan records:
  - `logs/anchor_ota_phase_20260331_scan_pre.json`
  - `logs/anchor_ota_phase_20260331_scan_post.csv`
- For UUID `CEE5A7EFCB35F8A56B430047629F5309`, observed unchanged:
  - anchor label: `B`
  - role: `responder`
  - stable UUID unchanged

### Important issue discovered in this run
- In OTA log, selected OTA peer was not constrained strongly enough:
  - `Scan match ... token=111 name=- ... accept=1 ...`
  - indicates current `master_ota` target acceptance can accept a peer even when configured name is set but absent in parsed AD name.
- Conclusion:
  - OTA transport path works end-to-end.
  - Wrong-target protection for anchor OTA is not yet strict enough.
  - This must be fixed before marking anchor OTA lifecycle as production-safe.

### Host BLE control helper robustness patch
- Updated `scripts/ble_anchor_control.py` to prefer connecting using resolved BLEDevice object and keep cleaner target serialization.
- In this environment, BlueZ still intermittently returns `device ... not found` at connect stage for random-address anchors; helper robustness improved but host BLE stack remains a bench limitation.

## 6) Identity-safe OTA targeting hardening + loop validation

### Why this phase
- Previous live run showed wrong-target risk in OTA peer selection.
- Objective in this phase:
  - enforce strict identity-safe OTA selection
  - strengthen 52840 control-center model
  - run repeated trials for convergence evidence

### Code changes
- Strict OTA identity gating in `master_ota`:
  - `apps/master_ota/src/main.c`
  - `apps/master_ota/src/master_ota.h`
- Added `ota_target uuid` path in `master_control`:
  - `apps/master_control/src/main.c`
- Added repeated OTA selection loop helper:
  - `scripts/loop_test_ota_targeting.py`

### Key design changes (implemented)
1. OTA now has explicit stable identity target field:
   - runtime target UUID (`32 hex`) is supported.
2. Hard rule enforced:
   - no verified UUID match => no OTA start.
3. Scan acceptance changed from permissive to strict:
   - name match is no longer optional when configured.
   - UUID match is mandatory for destructive OTA.
4. Selected peer verification state is carried into OTA start gate:
   - `selected_identity_verified` must be true before upload starts.
5. 52840 command parser supports full UUID command length:
   - fixed `arg2` parsing buffer from 31-char truncation to full 32-char UUID command.

### Build/flash evidence
- Updated 52840 control build:
  - `build-master-control-anchor-ota-safe-20260331b`
  - log: `logs/anchor_ota_phase_20260331_build_master_control_safe_b.log`
- Flash log:
  - `logs/anchor_ota_phase_20260331_flash_52840_safe_b.log`

### Loop-test methodology
- Script:
  - `scripts/loop_test_ota_targeting.py`
- Per trial:
  1. flash 52840 control image
  2. scan multi-device snapshot pre-trial
  3. set `device kind anchor`
  4. set `ota_target uuid` (+ optional name/token)
  5. switch `mode ota`
  6. capture scan/connect/upload evidence
  7. snapshot post-trial and compare non-target drift
- Loop output:
  - per-trial logs and a `summary.json` with convergence fields.

### Loop-test results (this run)
- Run:
  - `logs/anchor_ota_phase_20260331_loop_uuid_b/summary.json`
- Inputs:
  - target UUID: `D8757CA15A1837997DD3DB09F3B29C35`
  - target name: `ANCHOR-E-BS2B67`
  - trials: `3`
- Outcome:
  - `3/3` trials: no unintended connect (`no_connect_event`)
  - `0/3` wrong-target OTA starts
  - `0/3` non-target drift observed in scan snapshots
- Interpretation:
  - wrong-target permissive behavior did not reproduce after strict gating.
  - bench currently did not present a connectable OTA candidate matching the strict target, so upload did not start in these loop trials.

### Additional safety-check evidence
- No-UUID command path now accepted as config but does not provide identity-safe start:
  - `logs/anchor_ota_phase_20260331_strict_block_no_uuid.log`
- UUID command accepted correctly (after parser length fix) and persisted across reboot stage in control flow:
  - `logs/anchor_ota_phase_20260331_loop_uuid_b/trial_01/ota_trial.log`

### Current status
- Identity-safe OTA target model: implemented in 52840 OTA path.
- Wrong-target permissive acceptance gap: addressed in code path.
- Repeated-run safety behavior: consistent in loop logs (no ambiguous auto-selection).
- Remaining bench limitation:
  - loop environment did not provide repeatable strict-target OTA connection in this run, so convergence is demonstrated on safety gating (no wrong target), not yet on repeated successful strict-target uploads.

## 7) Strict-target OTA execution convergence (completed in this phase)

### Root cause analysis for prior `no_connect_event`
Observed failure source was not baseline architecture:

1. `master_control` parser truncated UUID command payload to 31 chars (fixed).
2. After UUID gating was enabled, `master_ota` still hard-required `name/prefix` match.
3. OTA advertisements often had no name (`name=-`) even for correct UUID peer.
4. Because default prefix is `BS`, correct UUID peers were rejected with:
   - `uuid_ok=1`
   - `accept=0`

Evidence:
- `logs/anchor_ota_phase_20260331_loop_uuid_d1/trial_01/ota_trial.log`

### Fixes applied
- `apps/master_control/src/main.c`
  - increased UART arg parsing buffer to carry full 32-hex UUID.
  - `ota_target uuid` now accepts full UUID consistently.
- `apps/master_ota/src/main.c`
  - UUID-authoritative target model retained.
  - matching logic updated:
    - if UUID configured, UUID match is mandatory.
    - exact name only enforced if explicit name is configured.
    - prefix no longer blocks UUID-authoritative selection when name is absent in AD.
  - removed prefilter dependency and scan all advertisements with identity-first filter logic.
  - added explicit operation-state logs:
    - `OTA_STATE:target_configured`
    - `OTA_STATE:target_rejected`
    - `OTA_STATE:connect_pending`
    - `OTA_STATE:connected_verified`
    - `OTA_STATE:ota_uploading`
    - `OTA_STATE:ota_upload_complete`
    - `OTA_STATE:reboot_pending`
    - `OTA_STATE:post_verify_passed`

### Build/flash evidence
- Updated control build:
  - `build-master-control-anchor-ota-safe-20260331e`
  - log: `logs/anchor_ota_phase_20260331_build_master_control_safe_e.log`
- Flash log:
  - `logs/anchor_ota_phase_20260331_flash_52840_safe_e.log`

### Loop-test methodology (execution convergence)
- Tool: `scripts/loop_test_ota_targeting.py`
- Target UUID: `D8757CA15A1837997DD3DB09F3B29C35`
- Each trial performs:
  1. flash control image
  2. set strict UUID target
  3. switch to OTA
  4. require selected UUID == target UUID
  5. require upload complete
  6. require post-OTA rediscovery
  7. record non-target drift check

### Loop-test results
- Single proof trial:
  - `logs/anchor_ota_phase_20260331_loop_uuid_e1/summary.json`
  - strict-target OTA succeeded end-to-end.
- 3-trial convergence run:
  - `logs/anchor_ota_phase_20260331_loop_uuid_e3/summary.json`
  - `trial_count=3`
  - `successful_trials=3`
  - `ota_completed_count=3`
  - `wrong_target_trials=0`
  - `non_target_drift_trials=0`
  - `safety_converged=true`
  - `execution_converged=true`

### Post-OTA control-plane persistence proof
- Post-OTA BLE control read/write/commit/readback evidence for target UUID:
  - write+commit:
    - `logs/anchor_ota_phase_20260331_ctrl_write_postota.json`
  - readback after reboot window:
    - `logs/anchor_ota_phase_20260331_ctrl_readback_after_reboot_retry.json`
- Verified:
  - stable UUID preserved
  - active/pending config still readable
  - generation/label/role persisted in BLE control plane after OTA cycle.

### Convergence status in this phase
- Safety convergence: **yes**
- Strict-target execution convergence: **yes (3/3)**
- Wrong-target regression: **none observed**

## 2026-03-31 Late rerun: anchor OTA flow + AutoPos V2 + Tag115 RECV check

### Request scope executed
- Re-run unified-anchor OTA/control flow to keep A..H visible in normal BLE scanner.
- Re-run AutoPos V2 matrix-to-layout pipeline.
- Re-check Tag115 (`BSF66F`) coordinate receive path on 52840.

### A..H scanner visibility (current state)
- Command:
  - `python3 scripts/scan_and_map.py --timeout-s 10`
- Artifact:
  - `logs/scan_anchor_now_20260331_rerun.csv`
- Result:
  - all 8 anchors visible with expected labels/roles:
    - A/B/C/D/E/F/G/H, role=`responder`
  - stable UUIDs are present per anchor.

### Anchor OTA flow re-run (strict target, Anchor B UUID)
- Command:
  - `python3 scripts/loop_test_ota_targeting.py --skip-flash --target-uuid B9179575C776C98F1CB132DD6EDC6223 --trials 1 --trial-timeout-s 120 --out-dir logs/ota_flow_rerun_anchorB_20260331_skipflash_224528`
- Artifacts:
  - `logs/pre_next_chain_20260331_anchor_ota_trial_skipflash.log`
  - `logs/ota_flow_rerun_anchorB_20260331_skipflash_224528/summary.json`
  - `logs/ota_flow_rerun_anchorB_20260331_skipflash_224528/trial_01/ota_trial.log`
- Result:
  - target selection safety passed (`selected_uuid` matched target, verified=1, wrong_target_started=0),
  - but this rerun did not enter upload (`ota_started=0`, `ota_completed=0`, notes=`incomplete`).

### 52840 control flash used in this rerun
- Successful flash with pinned SNR fallback:
  - `scripts/flash_master_noninteractive.sh build-master-control-anchor-ota-20260331f/master_control/zephyr/zephyr.hex`
- Artifact:
  - `logs/pre_next_chain_20260331_flash_master_otaf.log`

### AutoPos V2 rerun
- Command:
  - `python3 scripts/prepare_autopos_v2.py --pairs-csv logs/v2_round1_live_20260331_20260331_102808/matrix/pairs_all.csv --out-dir logs/autopos_v2_rerun_20260331_224507 --ref115-cm-baseline logs/master_ble_sessions/ref115_cali_ota_check_20260330_233748_cm360/cm_baseline_compact.json`
- Artifacts:
  - `logs/pre_next_chain_20260331_autopos_v2.log`
  - `logs/autopos_v2_rerun_20260331_224507/v2_fused/final_pair_distances_v2.csv`
  - `logs/autopos_v2_rerun_20260331_224507/v2_fused/pair_decision_report_v2.json`
  - `logs/autopos_v2_rerun_20260331_224507/v2_fused/anchor_layout_v2_iterative.json`
  - `logs/autopos_v2_rerun_20260331_224507/v2_prep_manifest.json`
- Result:
  - V2 pipeline completed successfully.
  - solver output around RMS ~69 mm on this dataset.

### Tag115 (`BSF66F`) BLE coordinate receive rerun status (52840)
- RECV capture attempts:
  - `logs/master_ble_sessions/rerun_tag115_recv_20260331_nors_223816`
  - `logs/master_ble_sessions/rerun_tag115_recv_conn_20260331_224054`
- OTA-mode receive attempt:
  - `logs/master_ble_sessions/rerun_tag115_ota_recv_20260331_224828`
- Control command logs:
  - `logs/pre_next_chain_20260331_master_cmd.log`
  - `logs/pre_next_chain_20260331_master_cmd_conn.log`
  - `logs/pre_next_chain_20260331_master_cmd_conn_afterflash.log`
  - `logs/pre_next_chain_20260331_master_cmd_ota_tag115.log`
  - `logs/pre_next_chain_20260331_force_connect_tag115.log`
- Observed blocker:
  - In RECV `conn`, controller repeatedly connected to anchor peer `BS0141` (example `FD:64:2E:93:37:86` / `F1:B3:DF:83:B1:C9`) instead of Tag115 `FC:9A:15:34:AB:20`.
  - Connected peer had no NUS service (`Could not start NUS discovery: -120`, `NUS service not found`), so no TS/coordinate stream.
  - Force-connect loop did not converge to Tag115 in 16 retries.
- Net result:
  - coordinate stream not obtained in this rerun (`unique_position_samples=0` in all three capture sessions).

## 2026-03-31 Receiver-mode fix on 52840 (BS0141 mis-parse + wrong peer selection)

### Problem confirmed
- User report: RECV `conn` often attached to anchor peers and then failed NUS discovery.
- Serial symptom:
  - `Connected[...] ... bs=BS0141`
  - `Could not start NUS discovery: -120`
- Root cause in code:
  - `apps/master/src/master_multi_app.c` legacy manufacturer parser interpreted new anchor MFG payload (`BSA...`) as old tag payload, producing fake `BS0141`.
  - RECV scan-connect path accepted any connectable peer with parsed `bs_code`, so anchors were eligible.

### Fix implemented
- File changed:
  - `apps/master/src/master_multi_app.c`
- Changes:
  1. Legacy MFG parser guard tightened:
     - only parse token/tag/bs_code for old payload shape `FF FF 'B' <token> ...`
     - explicitly reject new payload where byte 3 is `'S'` (`"BSA"` layout).
  2. Added NUS UUID detection helper (`ad_has_nus_uuid`).
  3. Updated scan logging to include `nus=<0|1>`.
  4. Added receiver candidate gate:
     - RECV auto-connect now only considers peers matching at least one tag capability signal:
       - `name_match || nus_match || dfu_match`
     - prevents anchor latching in normal tag receive flow.

### Build and flash
- Build:
  - `bash scripts/build_master_control.sh build-master-control-recvfix-20260331`
  - log: `logs/build_master_control_recvfix_20260331.log`
- Flash:
  - `scripts/flash_master_noninteractive.sh build-master-control-recvfix-20260331/master_control/zephyr/zephyr.hex`
  - log: `logs/flash_master_recvfix_20260331.log`

### Validation evidence
- Post-flash RECV/CONN check:
  - log: `logs/recvfix_conn_check_afterflash_20260331.log`
  - result:
    - scanner now reports Tag115 correctly:
      - `SCAN hit ... bs=BSF66F ...`
    - `conn` attaches to Tag115 address `FC:9A:15:34:AB:20` (not anchor)
      - `Connected[0]: ... bs=BSF66F`
- 120s receive capture:
  - session: `logs/master_ble_sessions/recvfix_tag115_recv_20260331_231245`
  - summary: `unique_position_samples=1`
  - raw evidence line:
    - `BSF66F notify: TS;1;3536;t;2520;396;471;26;34;ADEF;...`

## 2026-04-01 00:xx — 52840 command test: switch Tag115 to PMODE=4

### Goal
- Use 52840 BLE central path to switch Tag115 (`BSF66F`) into `PMODE=4` (Anchor-OTA quiet mode).

### Actions
- Built/flash 52840 `master_control` with one-shot BLE command variants:
  - `MODE AOTA`
  - `CFG PMODE=4`
  - `HELP` (capability probe)
- Forced reconnect cycle each time (`scan` then `conn`) to ensure one-shot command was sent after fresh link-up.

### Evidence
- Workspace:
  - `logs/tag115_pmode4_via_52840_20260401_000604/`
- Key logs:
  - command sent (`MODE AOTA`) + target connected `BSF66F`:
    - `serial_scan_conn_oneshot.log`
  - command sent (`CFG PMODE=4`) + target connected `BSF66F`:
    - `serial_scan_conn_cfg_pmode4.log`
  - capability probe:
    - `serial_scan_conn_help.log`

### Observed result
- 52840 reliably connected to Tag115 (`Connected[0] ... bs=BSF66F`) and sent one-shot commands.
- Tag115 rejected both PMODE4 commands in current runtime image:
  - `MODE AOTA` -> `MODE_BAD`
  - `CFG PMODE=4` -> `CFG_BAD`
- Therefore PMODE4 switch did **not** take effect through current BLE command set on this running Tag115 firmware.

## 2026-03-31 late continuation — PMODE remap fix + unified 52840 control consolidation

### Scope
- Keep date in `20260331` log per operator request.
- Fix Tag115 AOTA mode index confusion (`PMODE` starts at `0`), and verify successful switch.
- Consolidate current 52840 behavior into one official unified control build path (scan/connect/receive/control/ota) without per-command reflashing.

### 1) PMODE mapping fix (code)
- Changed AOTA mode value from `4` to `3`:
  - `include/uwb_tdma.h`
    - `UWB_TAG_POSITIONING_MODE_ANCHOR_OTA = 3U`
- Updated BLE HELP text:
  - `apps/tag/src/uwb_tag_ble.c`
    - `PMODE=<0|1|2|3>`

### 2) Unified 52840 control runtime command path (code)
- Added runtime command APIs in receiver path:
  - `apps/master/src/master_multi_app.h`
  - `apps/master/src/master_multi_app.c`
    - `master_send_command_now()`
    - `master_set_one_shot_command()`
    - `master_clear_one_shot_command()`
    - `master_print_one_shot_command()`
    - discovery one-shot now uses runtime command when armed.
- Extended `master_control` UART parser:
  - `apps/master_control/src/main.c`
    - `cmd <raw NUS command>`
    - `oneshot <raw NUS command>`
    - `oneshot show`
    - `oneshot clear`
- Added official unified build helper:
  - `scripts/build_master_control_unified.sh`

### 3) Build artifacts (this run)
- Tag unified PMODE3 build:
  - `build-tag-ble-unified-pmode3-20260401a`
  - build log: `logs/build_tag_ble_unified_pmode3_20260401a.log`
- Unified 52840 control build:
  - `build-master-control-unified-tag115pmode3-20260401a`
  - build log: `logs/tag115_pmode3_fix_20260401_003909/build_master_control.log`

### 4) Why prior AOTA switch was failing
- Prior failure (`MODE_BAD` / `CFG_BAD`) happened while running image expected `PMODE=4`.
- After remap, runtime requires `PMODE=3` for AOTA.
- Additional OTA attempt blocker observed:
  - `master_ota` currently enforces UUID-authoritative OTA start (`strict_identity_ok`).
  - Tag115 scan records show `uuid=-`, so strict OTA start is blocked (safe reject).
  - Evidence:
    - `logs/tag115_pmode3_fix_20260401_003909/step1_ota_to_tag115.log`
    - `logs/tag115_pmode3_fix_20260401_003909/step1b_ota_to_tag115_token111.log`

### 5) Tag115 deployment + PMODE switch verification (successful)
- Direct deploy of updated Tag115 image (bench recovery path):
  - flash log: `logs/tag115_pmode3_fix_20260401_003909/flash_tag115_direct.log`
- 52840 receiver/control verification:
  - log: `logs/tag115_pmode3_fix_20260401_003909/step2_mode_switch_verify_after_direct_flash.log`
  - key lines:
    - `Connected[0] ... bs=BSF66F`
    - `BLE cmd sent[0]: MODE AOTA`
    - `BSF66F notify: MODE_OK MODE=AOTA LIVE=1`
    - `BSF66F notify: MODE=AOTA PMODE=3 ...`
    - `BSF66F notify: CFG ... pmode=3 ...`
- Result:
  - Tag115 mode switch to AOTA via 52840 BLE command path is now verified with `PMODE=3`.

### 6) Consolidation status (52840)
- Unified build now covers:
  - scan / connect / receive / runtime control dispatch / ota mode
- Receiver-side behavior switching and command injection now run-time (no reflashing needed per command).
- Remaining known limitation:
  - OTA mode still requires identity-safe target evidence; for peers without UUID in advertisements, OTA remains blocked by policy.

## 2026-03-31 late continuation (interrupt recovery) — strict anchor OTA execution debug

### Scope
- Resume from interrupted full-chain run and focus on Step01 blocker: strict UUID-targeted Anchor OTA completion.
- Keep identity-safe behavior (no permissive fallback) and avoid architecture expansion.

### What was executed
- Re-ran strict UUID-targeted Anchor A OTA trials with current 52840 runtime:
  - `logs/full_chain_20260401_20260401_012620/step01_anchor_ota/fix_h1/ota_A_trial_live4/trial_01/ota_trial.log`
- Added trial-script robustness so each trial force-cycles control mode:
  - `scripts/loop_test_ota_targeting.py`
  - changed flow to `mode recv -> mode ota` and delayed `mode ota` send to avoid reboot-window command loss.
- Added serial buffer reset in trial runner to avoid concatenated UART commands.

### Current strict OTA status (after fixes above)
- Safety gate is working:
  - target filtering by UUID is strict
  - wrong peers are rejected
  - intended peer connects with `Connected target evidence: verified=1`.
- Execution still blocked at SMP command phase:
  - `OTA prime echo request`
  - `OTA command wait failed ... rc=-116`
  - `OTA erase secondary slot request`
  - `OTA erase failed: -116`
- So status is:
  - safety convergence: **yes**
  - successful end-to-end strict OTA execution: **not yet**

### Additional code patch prepared (not yet deployed to 52840 due J-Link issue)
- `apps/master_ota/src/main.c`
  - prefer write-without-response for SMP command path, fallback to write request, add write callback error checks.

### Bench blocker observed
- 52840 debug probe (`683234364`) repeatedly fails nrfjprog operations with JLink error `-256`:
  - mem read/flash/recover all fail in current bench state.
- Evidence:
  - `logs/full_chain_20260401_20260401_012620/step01_anchor_ota/fix_h1/flash_52840_20260401h.log`
  - `logs/full_chain_20260401_20260401_012620/step01_anchor_ota/fix_h1/recover_52840_20260401h.log`

### Interim conclusion
- The remaining gap is no longer target safety logic; it is strict OTA execution reliability plus current debug-link instability for deploying the latest 52840 patch build.

## 2026-03-31 continuation (four-step debug: same image, config/state, SMP path)

### Trigger question
- Why USB/JLink flashed anchor image can be discovered for A..H, but strict OTA still misses/fails on some anchors.

### Step 1 — normalize A/D with same runtime path and retry strict OTA
- Actions:
  - Re-provisioned A and D with identical role policy (`responder`) and verified readback.
  - Re-ran strict UUID-target OTA trials for A and D.
- Evidence:
  - `logs/debug_fourstep_20260401_step1b_20260401_145042/`
  - `.../provision_A.log`, `.../provision_D.log`, `.../readcfg_A.json`, `.../readcfg_D.json`
  - `.../ota_A/trial_01/ota_trial.log`, `.../ota_D/trial_01/ota_trial.log`
- Result:
  - Still fails at SMP phase (`OTA erase failed: -116`), so not solved by simple A/D config normalization.

### Step 2 — add anchor-side MCUmgr/SMP diagnostic hooks
- Code changes:
  - Added `src/anchors/unified/anchor_mcumgr_diag.[ch]`
  - Integrated in `apps/anchor/src/anchor_app.c`
  - Enabled mcumgr notification hooks in `apps/anchor/prj_ota.conf`
  - Added source in `apps/anchor/CMakeLists.txt`
- Build:
  - `build-anchor-unified-ota-diag-20260401`
  - build log: `logs/build_anchor_ota_diag_20260401.log`
- Runtime evidence on Anchor A:
  - `anchor mcumgr diag callbacks registered` appears.
  - During strict OTA fail window: no `MCUMGR_SMP_EVT` / `MCUMGR_IMG_EVT` entries observed.
  - Evidence:
    - `logs/debug_fourstep_20260401_step2b_20260401_145932/anchorA_serial.log`
    - `.../ota_A/trial_01/ota_trial.log`

### Step 3 — compare working responder (Tag115) vs failing anchor path
- Direct BLE SMP echo tests:
  - Tag115 (`BSF66F`) responds immediately on same characteristic (`DA2E...`).
  - Anchor A did not respond under responder load path.
- Additional runtime-state test:
  - A switched to `matrix` (`allow_tag_polls=0`) then direct SMP echo produced notify response in one run.
- Evidence:
  - Tag response:
    - command output in terminal run (`notifies 1`, payload returned).
  - A responder no-response:
    - `logs/debug_fourstep_20260401_step3_20260401_150246/direct_smp_to_A.log`
  - A matrix test:
    - `logs/debug_fourstep_20260401_step3b_20260401_150436/direct_smp_to_A_matrix.log`
    - `.../anchorA_matrix_serial.log`
- Interpretation:
  - Strong indication that OTA/SMP reliability is affected by runtime state/load (especially responder poll traffic), not by “different image binary” mismatch.

### Step 4 — attempted practical fix + 8-anchor strict OTA regression
- Practical fix attempt:
  - Set all A..H to `matrix` (quiet mode for tag polls), verify readback.
- Evidence:
  - `logs/debug_fourstep_20260401_step4b_matrix8_20260401_150748/`
  - per-anchor provision/readcfg logs.
- Strict OTA all8 regression run (UUID strict):
  - `logs/debug_fourstep_20260401_step4c_strictota8_20260401_151001/results.csv`
- Result summary:
  - safety: wrong-target remains 0
  - execution: still not converged (all entries reported `no_connect_event` at summary layer; trial logs still show OTA entering upload state then timeout/fail patterns)

### Current technical conclusion
- This is not explained by “OTA image differs from USB/JLink-flashed image”.
- The remaining blocker is execution-side SMP reliability under current runtime/connection behavior.
- Next required fix direction:
  - stabilize OTA preconditions into explicit quiet window (disable tag poll interference during OTA), and
  - harden 52840-side trial/result parser so `no_connect_event` does not mask actual `connected-but-timeout` cases.

## 2026-04-01 (recorded under 20260331) — RECV->DISCONN->OTA->INITIATE strict flow test (Anchor A)

- Requested flow executed:
  - `RECV -> DISCONNECT ALL -> OTA -> INITIATE -> OTA scan/connect`
- Firmware update:
  - `apps/master_control/src/main.c`
    - Added mode-switch preflight disconnect-all before rebooting mode.
    - Added UART command `initiate` in OTA mode.
  - `apps/master_ota/src/master_ota.h`
    - Added `int master_ota_initiate(void);`
  - `apps/master_ota/src/main.c`
    - Added `master_ota_initiate()` to reset OTA runtime state, disconnect links, restart passive scan, and set operation state to `connect_pending`.
  - `scripts/loop_test_ota_targeting.py`
    - Updated to trigger `mode ota` then `initiate` (no RECV fallback).

### Build/flash evidence (52840)
- Build log: `/home/zekaixiao/Documents/nRF_dev/BioSpur_UWB_before_start/logs/build_master_control_unified_cdc_20260401e_initiate.log`
- Flash log: `/home/zekaixiao/Documents/nRF_dev/BioSpur_UWB_before_start/logs/flash_52840_unified_cdc_20260401e_initiate.log`

### Runtime evidence (Anchor A strict-target UUID)
- Session: `/home/zekaixiao/Documents/nRF_dev/BioSpur_UWB_before_start/logs/debug_anchorA_initiateflow_20260401_163836`
- Key log: `/home/zekaixiao/Documents/nRF_dev/BioSpur_UWB_before_start/logs/debug_anchorA_initiateflow_20260401_163836/trial_01/ota_trial.log`
- Observed sequence:
  - `Mode switch preflight: disconnect requests=1`
  - `initiate rc=0`
  - `OTA initiate: scan restarted (passive)`
  - Strict UUID filtering rejects non-target peers.
  - Target A selected and verified:
    - `Connect start: F2:B7:4E:8A:79:01 ...`
    - `Connected target evidence: verified=1 uuid=4DC6B8187E33803AE8601FB0D7992B96`
  - OTA still fails at SMP command phase:
    - `OTA command wait failed ... rc=-116` (prime and erase)

### Current status
- Flow-control logic improved and now reaches verified target A deterministically.
- Remaining blocker is unchanged: OTA SMP response timeout (`-116`) after verified connect.
## 2026-04-01 Strict OTA Debug Resume (from interrupt point)

### Scope
- Resume strict-target Anchor A OTA debug without probe-selection popup usage.
- Keep explicit-SNR/JLink non-interactive flow only.

### Actions
- Reproduced strict-target run from breakpoint:
  - log: `logs/debug_anchorA_initiateflow_20260401_163836/trial_01/ota_trial.log`
  - result: target UUID verification PASS, wrong-target start = 0, OTA SMP wait timeout `-116` persists.
- Added OTA control-side connection diagnostics:
  - file: `apps/master_ota/src/main.c`
  - added `bt_conn_set_security(..., BT_SECURITY_L2)` request and `security_changed` logs.
- Rebuilt + flashed unified 52840 control build:
  - build log: `logs/build_master_control_unified_cdc_20260401e_secfix.log`
  - flash log: `logs/flash_52840_unified_cdc_20260401e_secfix.log`
- Ran clean manual strict-target sequence with explicit `mode ota -> initiate`:
  - log: `logs/debug_anchorA_secfix_manual_20260401.log`

### Key findings (new)
- Identity-safe targeting still works:
  - connected target UUID stays `4DC6B8187E33803AE8601FB0D7992B96`
  - no wrong-target destructive start observed.
- Link-level anomalies observed before/around DFU:
  - `Security changed ... level=1 err=9/5` after L2 request.
  - one cycle had `MTU exchange failed: 14` and disconnect `reason 0x3e`, then reconnect.
- Even after reconnect and `DFU SMP service ready`, SMP commands still time out:
  - `[...->...] OTA command wait failed ... rc=-116`
  - `OTA wait debug: rsp_len=0 rsp_total=0 chunk=0 off=0`
  - implies no SMP notification response arrived.

### Current blocker
- Strict target selection is converged.
- OTA execution remains blocked at SMP response path (`-116`) after verified connect.
- Next fix focus should be SMP transport acceptance/response readiness on target link, not identity gating.

### Additional hardening + rerun (same day)
- Applied OTA-control flow hardening:
  - OTA no longer auto-starts on OTA mode entry; requires explicit `initiate`.
  - loop harness now sends `initiate` after `mode ota` and serial re-open.
  - files:
    - `apps/master_ota/src/main.c`
    - `scripts/loop_test_ota_targeting.py`
- Evidence run:
  - `logs/debug_anchorA_armfix_20260401/trial_01/ota_trial.log`
  - strict UUID match remains PASS.
  - wrong-target start remains 0.
  - still fails at SMP wait timeout:
    - `OTA command wait failed ... rc=-116`
    - `rsp_len=0 rsp_total=0 chunk=0 off=0`
- Additional observed diagnostics during strict runs:
  - `Security changed ... level=1 err=5/9` after L2 request.
  - intermittent `DFU SMP handle assign failed: -22` on a transient cycle before reconnect.

## 2026-04-01 Comparative strict OTA trial: H (control) vs A/D

### Objective
- Re-run identical strict-target OTA flow on:
  - H (expected functional control sample)
  - A
  - D
- Compare whether A/D are uniquely broken or part of broader OTA execution issue.

### UUID mapping used
- A = `4DC6B8187E33803AE8601FB0D7992B96`
- D = `AB14CCA262A092E70EB26B0ACB0A394B`
- H = `1EABFBEC28B8053FBB0D5C448112AE93`
- Source snapshot examples:
  - `logs/full_chain_20260401_20260401_012620/step01_anchor_ota/strict_uuid/ota_H/trial_01/scan_pre.csv`
  - `logs/full_chain_20260401_20260401_012620/step01_anchor_ota/strict_uuid/ota_A/trial_01/scan_pre.csv`
  - `logs/full_chain_20260401_20260401_012620/step01_anchor_ota/strict_uuid/ota_D/trial_01/scan_pre.csv`

### Trial artifacts
- H:
  - `logs/compare_strict_ota_20260401_H.runner.log`
  - `logs/compare_strict_ota_20260401_H/trial_01/ota_trial.log`
  - `logs/compare_strict_ota_20260401_H/summary.json`
- A:
  - `logs/compare_strict_ota_20260401_A.runner.log`
  - `logs/compare_strict_ota_20260401_A/trial_01/ota_trial.log`
  - `logs/compare_strict_ota_20260401_A/summary.json`
- D:
  - `logs/compare_strict_ota_20260401_D.runner.log`
  - `logs/compare_strict_ota_20260401_D/trial_01/ota_trial.log`
  - `logs/compare_strict_ota_20260401_D/summary.json`

### Results
- All three targets passed strict identity safety:
  - selected UUID == requested UUID
  - `verified=1`
  - `wrong_target_started=0`
- All three failed OTA execution with the same terminal condition:
  - `OTA command wait failed ... rc=-116`
  - `rsp_len=0 rsp_total=0 chunk=0 off=0`
  - `OTA erase failed: -116`

### Comparative observations
- H:
  - clean connect + MTU 247 + DFU SMP ready, then `-116` timeout.
- A:
  - same pattern as H overall; reaches DFU SMP ready and still times out.
- D:
  - one extra unstable cycle (`security err=9`, `MTU exchange failed: 14`, disconnect `0x3e`), then reconnects to DFU-ready state and still hits same `-116`.

### Conclusion from compare run
- A and D are **not** uniquely failing at strict-target selection.
- D has additional link instability, but final OTA execution failure (`-116` with zero SMP response) is shared by H/A/D.
- Current blocker remains system-level in OTA SMP response path after verified connect, not an A-only or D-only identity problem.
