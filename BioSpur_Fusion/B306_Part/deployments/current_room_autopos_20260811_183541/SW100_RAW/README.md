# SW100 raw evidence pointer

The authoritative raw SW100 evidence is retained without duplication in
`../SW100/`: eight `round_<A-H>/master.log` files plus the session role-guard,
final-responder log, and `summary.json`. `../V4IO/pairs_all.csv` is the
deterministically extracted solver input. No smoke or retry run is mixed into
this deployment.

