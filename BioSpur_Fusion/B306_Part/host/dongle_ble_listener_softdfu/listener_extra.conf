# Fusion-project build overlay for the independent nRF52840 dongle observer.
# Keep the allocator finite so this diagnostic image passes the production
# memory gate. The listener source already implements the 1200-baud software
# entry into Nordic Open DFU.
CONFIG_COMMON_LIBC_MALLOC_ARENA_SIZE=0
