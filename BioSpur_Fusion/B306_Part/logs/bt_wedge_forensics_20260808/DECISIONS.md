# DECISIONS — forks taken autonomously

Per the autonomy clause: every fork, the choice, one line of rationale.

| # | fork | choice | why |
|---|---|---|---|
| 1 | `pyarrow` absent and `pip` refuses to install into the system Python (PEP 668) | created a venv with `--system-site-packages` at `<scratchpad>/venv` | least invasive; system Python untouched. All analysis runs under that interpreter. |
| 2 | Which directory is "N5"/"N7"/"N8" | `B306_Part/logs/v43_selfcapture_20260807/B5_RUN`, `UWB_Part/logs/daylight_20260807/B_RUN`, `UWB_Part/logs/v44_fleet_20260807/I_RUN` | only directories matching the brief's names and firmware; confirmed by node set, duration and record-type mix. |
| 3 | A fourth v43 run exists (`B306_Part/logs/v43_run2_20260807/B_RUN`, 38 s, 10 nodes) that the brief does not mention | included as **N6**, run through the identical detector, and reported separately | excluding a run because the brief did not name it would bias the exposure denominator. It contributes 0.11 bh and 0 events. |
| 4 | `channel.log` duplicates `fusion_h*` | listed in the manifest, never parsed | brief §prohibitions. Verified duplicate by record-type mix and by size (2560 MB vs 2503 MB of `fusion_h*` for N8; the delta is the `FUSION_RX` prefix). |
| 5 | Large-file hashing | `<1 GiB` full sha256; `≥1 GiB` head-1 MiB + tail-1 MiB + exact size | full hashing of 50 GiB would cost ~20 min of wall time for no analytical gain; scheme documented in the manifest. |
| 6 | `FUSION_MASTER_POOL` is emitted as 10 physical lines (one per pool), so its continuation lines look like record types | continuation lines land in `ctl_<run>.jsonl` as noise and are ignored | master-side pools are out of scope; node `FUSION_POOL` is single-line and unaffected (verified: 34 251/34 251 node pool lines carry `pools=`). |
| 7 | `low_water` semantics | **corrected mid-run.** First read as a per-second window minimum (the source comment says so); re-read the write sites and found `pool_low_water[]` is touched at exactly `main.c:523/538` (1 Hz) and `main.c:3530` (init) — no sub-second observer exists. It is a **two-point 1 Hz strobe**. | the stronger reading would have made §9 claim more than the instrument can support. The correction is what prompted the unbiased `FUSION_STALL_POOLS` census, which then inverted the leading hypothesis' premise. Fixed before any conclusion depended on it. |
| 8 | Near-miss floor `T_near` | **2 s**, with 0.5/1/2/4/8 s sensitivity reported | 0.5 s is inside normal jitter (835 hits in N5 alone); 2 s is above the healthy tail and below the 4 s supervision timeout. |
| 9 | Reboot test for classification | reboot **at onset** (−30/+60 s), not anywhere in the stall window | the loose form misclassified BSFEC35's 15:46 wedge as brownout on the strength of its own 21:14 depletion reboot 5½ h later. Caught in the first pass and fixed. |
| 10 | Air-ratio thresholds | 0.70 / 0.15 as specified; sensitivity reported | the observed clusters are 0.959–1.096 (n=4) and 0.001–0.185 (n=11); any threshold in [0.25, 0.90] gives the same partition. |
| 11 | Two listeners have zero `LPD` records | skipped **with a note**, not fatal | they are a beacon transmitter and a beacon receiver; "no data" ≠ "failed". A listener that *has* LPD but cannot be calibrated remains fatal. |
| 12 | Listener clock calibration | two-point affine from head/tail `listener_t_ms` ↔ `arrival_epoch_ns` in the per-unit `.jsonl` | `merged_index.jsonl` is forbidden and unnecessary. Residual over 6–8 h is seconds; every question asked of the air timeline is minutes-scale. Measured slopes are −68 to −87 ppm, consistent across all five units, i.e. a common-mode host/listener offset, not per-unit drift. |
| 13 | Exposure denominator | Σ over nodes of (last delivered − first delivered) − non-terminal stall time | terminal stall time is post-mortem and must not count as exposure; matches the brief's "linked ∧ delivering". |
| 14 | Which run's baseline to fit "healthy" against for N8 | the at-capacity window `run start → 17:28:20` where available; otherwise matched same-window controls | per §0.2. Where a node's onset is outside that window (BSF44AD 19:51, both depletion clusters) the matched-control approach is used instead and said so. |
| 15 | `pandas` version is 3.0.3, whose API differs from 2.x in places | used `pyarrow` + `numpy` directly for all heavy work; `pandas` only for tabular output | avoids silent behaviour changes. |
| 16 | Node-internal trigger base rates need sub-second ordering that 1 Hz telemetry cannot give | computed **enrichment** only, and stated the resolution limit explicitly | per §7.2's own instruction. |
| 17 | The brief says all writes go inside the output directory; the venv and one intermediate word list live in the session scratchpad instead | kept them there, recorded here | a Python venv is a tool, not an analysis artifact, and the scratchpad is the harness-designated temp area. **No write of any kind touched a capture directory or the repo tree outside this output directory.** Every input was opened read-only. |
| 18 | Two ad-hoc control-plane queries early in P5 used hand-typed onset epochs that were four hours wrong | caught by an implausible result (`not_connected` rejects appearing 109 ms after an onset that should have had a live link), re-run against the registry | the scripted analyses (`p3`–`p6`) all read onsets from `WEDGE_EVENTS.json` and were never affected. The corrected timelines are what `COMMAND_TRIGGER_ANALYSIS.md` and `DOWNTIME_LEDGER.md` report. |
