# Historical verdict boundary

- `BLOCKED_INSUFFICIENT_EXCITATION` remains unchanged and applies only to its frozen historical model.
- `BLOCKED_ROTATION_AWARE_MODEL_UNOBSERVABLE` remains unchanged and applies only to its frozen historical model.
- `C2CC_FRAME_BINDING_FITTING_RECOVERED_PROSPECTIVE_VALIDATION_REQUIRED` remains unchanged and applies only to its frozen historical model.

This v1 natural-motion analysis is additive; it does not rewrite or relabel those artifacts.
