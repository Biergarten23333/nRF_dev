# Phase 3 plan deviation record: operator mapping

Decision authority: explicit user product decision in the Phase 3 execution
authorization. Scope: the targeted capture `Capture_2_with_JOINT_LABEL`, session
`capture_2_with_joint_label`, and its recorded donning only.

The Phase 2-R automatic association failure is accepted as a permanent result,
not waived or renamed. Phase 3 binds streams using the separately recorded
10-by-10 operator mapping. The estimator is provider-neutral after construction,
and runtime rebinding is forbidden.

The narrowly superseded v2.0 clauses are the AutoMapping-first interpretation of
section 17 (no operator mapping required), section 19 (`top-K -> freeze` only),
and section 20 (operator mapping as a no-go). All calibration, observability,
data-sealing, external-truth, accuracy, and publication requirements remain in
force. A new session without its own confirmed binding fails closed.

This deviation does not authorize Phase 4, UWB numeric access, AutoMapping
retuning, or any external pose, clinical-angle, or metric-world claim.
