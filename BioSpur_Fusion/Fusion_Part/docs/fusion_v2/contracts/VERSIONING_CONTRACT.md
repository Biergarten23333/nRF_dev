# Versioning contract

Namespace is `fusion_v2`. `spec_version`, `schema_version`, `profile_version`, `implementation_version` and Git SHA are independent. Historical meaning cannot change silently. Internal state schema is not HumanState; output schema is only a minimum safe future contract. No CanonicalHumanState v0.1 is frozen.
