# Data access policy

Only exact allowlisted realpaths may be opened through the audited wrapper. D3 values are never decoded, materialized, retained, plotted or statistically counted. D1 and D2 outputs are separate repo-external content-addressed deterministic gzip files.
