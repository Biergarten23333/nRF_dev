# Constrained ten-node body calibration

Final result: [`run_b/REPORT.md`](run_b/REPORT.md).

Top-level verdict: `BODY_MAPPING_CONSTRAINED_PASS_FUSION_READY`.

The historical anonymous analysis under `analysis_body_calibration_v1/` remains unchanged. `run_b` and `run_c` are independent, byte-identical complete scientific derivations. Large NPZ and diagnostic video artifacts remain outside Git; compact manifests and evidence pointers are committed.

Animations are under [`run_b/visualizations/`](run_b/visualizations/). They are post-freeze diagnostic renderings and do not change fitting, gates, or verdicts.
