# Evidence pointers

Scientific result directory: `run_b/`.

Large local-only products:

- `run_b/B0_UWB_TAG_T4_TIMELINES.npz`
- `run_b/F1_Q1_T4_BODY_TRAJECTORIES.npz`
- `run_b/visualizations/*.mp4`
- `run_b/visualizations/*_PREVIEW.gif`

Compact committed manifests retain provenance, freeze SHA, video SHA-256 values, encoding contract and exact local paths. The authoritative raw capture is referenced in the parent capture completion record and is never copied or modified.
