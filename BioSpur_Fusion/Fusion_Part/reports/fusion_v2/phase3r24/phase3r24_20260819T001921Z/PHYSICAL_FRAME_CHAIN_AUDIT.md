# Physical frame-chain audit

Classification: `CONFLICTING_OR_REVISION_UNBOUND`.

Using `R_AB` for B-to-A coordinates, the required chain is `R_Pprotocol_I(t_ref)=R_Pprotocol_Spelvis R_Spelvis_D R_DE R_EB R_BM R_MI`. Only the signed register decoder and VQF output convention close. The session record locates C2CC at the front belt center but does not identify a directed device face/edge, and no revision-bound C2CC enclosure/PCB/module transform or surveyed pelvis-to-protocol pose exists.

The nine H9 boards' approximate short-edge-down statement is vertical/tilt evidence and is explicitly inapplicable to C2CC. No IMU-derived direction was recycled as an anchor.
