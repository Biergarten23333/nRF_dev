# Profile safety contract

Projection result is exactly `EXACT`, `LOSSY_WITH_MANIFEST` or `REFUSED_UNREPRESENTABLE`. Mandatory gauge, validity, inference, cutoff, uncertainty and provenance semantics cannot be stripped when that would mislead.
