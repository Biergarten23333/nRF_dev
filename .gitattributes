# NOTE: logs/** are NO LONGER tracked by Git LFS (GitHub LFS quota).
# Raw overnight capture data (*.csv, *.log under broadcast logs) is gitignored
# and kept local-only; small results (summary.json, *.png) go to normal Git.
BioSpur_UWB_before_start/overnight_radar_20260710/raw/*.log filter=lfs diff=lfs merge=lfs -text
BioSpur_UWB_before_start/overnight_radar_20260710/analysis/parsed/*_cir.npy filter=lfs diff=lfs merge=lfs -text
